{
  "name": "omarchy-quiet-queue-entry",
  "version": "0.1.0",
  "private": true,
  "description": "Owner-aware Omarchy notification quiet-mode harness",
  "scripts": {
    "test": "c8 --all --include=Model.js --check-coverage --lines 95 --functions 95 --branches 90 node --test tests/*.test.js"
  },
  "devDependencies": {
    "c8": "^10.1.3"
  }
}
